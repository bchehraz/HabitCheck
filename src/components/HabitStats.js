import React from 'react';

import AppLayout from './AppLayout';

const HabitStats = ({ path }) => (
  <AppLayout path={path}>
    <h1>{`The "Habit Stats" Tab`}</h1>
  </AppLayout>
);

export default HabitStats;
