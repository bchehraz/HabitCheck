// import { handleToggleDarkMode, handleToggleXEffectView } from '../preferences'

// jest.mock('../auth');
// jest.mock('../helpers');

// describe("test preference toggle functions to properly return preferences object", () => {
//   it("should work", () => {
//     expect(handleToggleDarkMode()).toBe(true);
//   })
// })

it("should work", () => expect(true).toBe(true))
